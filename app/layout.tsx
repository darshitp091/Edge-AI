import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EdgeAI - Compress AI Models 10x Smaller | Deploy Anywhere",
  description: "Compress AI models 10x smaller with INT8/INT4 quantization. Deploy to edge devices, mobile, IoT. Save 90% on inference costs. Start free today.",
  keywords: ["AI compression", "model optimization", "edge AI", "quantization", "INT8", "INT4", "TensorFlow Lite", "ONNX", "PyTorch"],
};

import { Analytics } from "@vercel/analytics/react";
import { SpeedInsights } from "@vercel/speed-insights/next";

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Analytics />
        <SpeedInsights />
      </body>
    </html>
  );
}
